#@data/values
---
issuer:
  name: selfsigned-issuer
  namespace: system

certificate:
  name: serving-cert
  namespace: system
  inject_into:
    webhooks: true

service:
  name: webhook-service
  namespace: system

manager:
  image: controller:latest
  imagePullPolicy: Always


