#@ load("@ytt:overlay", "overlay")
#@ load("manager.lib.yml", "controller_manager")

#@overlay/match by=overlay.subset(controller_manager())
#@overlay/match-child-defaults missing_ok=True
---
spec:
  template:
    spec:
      containers:
        #@overlay/match by=overlay.subset({"name": "manager"}), missing_ok=True
        - image: gcr.io/k8s-staging-cluster-api/cluster-api-controller:master
          name: manager
